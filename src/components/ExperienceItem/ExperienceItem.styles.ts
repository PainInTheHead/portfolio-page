import styled from "styled-components";

const StyledExperienceItem = styled.div`
  .experience-item__subtitle {
    margin-bottom: 10px;
  }

  .experience-item__list {
    gap: 6px;

    font-size: 16px;
    line-height: 24px;
  }
`;

export default StyledExperienceItem;